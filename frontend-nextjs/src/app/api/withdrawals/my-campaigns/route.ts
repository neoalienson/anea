// This route is no longer needed as we handle campaign cancellations directly in the component
// The cancellation logic is now implemented in the individual campaign routes
// using NextAuth sessions and direct Supabase calls for consistency with the existing app pattern