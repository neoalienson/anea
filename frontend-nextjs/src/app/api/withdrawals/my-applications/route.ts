// This route is no longer needed as we handle withdrawals directly in the component
// The withdrawal logic is now implemented in the individual application/campaign routes
// using NextAuth sessions and direct Supabase calls for consistency with the existing app pattern